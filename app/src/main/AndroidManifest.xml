<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.udem.fruname">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".juegos.ruletaPreguntados"></activity>
        <activity android:name=".juegos.preguntados" />
        <activity android:name=".juegos.descubreLaImagen" />
        <activity android:name=".juegos.adivinarBandera" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tablero" />
        <activity android:name=".registrar" />
        <activity android:name=".login" />
    </application>

</manifest>